// ? primitive types in TS

//* string
let myName: string = "Bilal";

//* number
let num: number = 10;
num = 20;

//* boolean
let isUser: boolean = true;

//* null type
let users: null = null;

//* undefined type
let e: undefined;

//* Bigint
let f: bigint = BigInt(1999);

//* symbol type
let g: symbol = Symbol("Name");
